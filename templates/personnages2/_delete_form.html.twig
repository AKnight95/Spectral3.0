<form method="post" action="{{ path('app_personnages2_delete', {'id': personnages2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personnages2.id) }}">
    <button class="btn">Delete</button>
</form>
