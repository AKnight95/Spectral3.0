{% extends 'base.html.twig' %}

{% block title %}Personnages{% endblock %}

{% block body %}
    <h1>Personnages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personnage.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ personnage.Nom }}</td>
            </tr>
            <tr>
                <th>Histoire</th>
                <td>{{ personnage.Histoire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_personnages_index') }}">back to list</a>

    <a href="{{ path('app_personnages_edit', {'id': personnage.id}) }}">edit</a>

    {{ include('personnages/_delete_form.html.twig') }}
{% endblock %}
